# Core JAX/FLAX dependencies for SERL
jax>=0.4.20
jaxlib>=0.4.20
flax>=0.7.0
optax>=0.1.7
chex>=0.1.7
distrax>=0.1.3

# PyTorch (should already be installed with LeRobot)
torch>=1.11.0
torchvision>=0.12.0

# Additional dependencies
numpy>=1.21.0
scipy>=1.7.0
pillow>=8.3.0

# Logging and experiment tracking
wandb>=0.15.0
tensorboard>=2.8.0

# Optional: GPU acceleration (uncomment as needed)
# jax[cuda11_pip]>=0.4.20  # For CUDA 11 support
# jax[cuda12_pip]>=0.4.20  # For CUDA 12 support

# Development dependencies (optional)
# pytest>=6.0.0
# black>=22.0.0
# isort>=5.0.0
# flake8>=4.0.0 